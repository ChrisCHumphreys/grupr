<div>
  <div class="containter-fluid">
    <form
       action="{% url 'walk-create' %}"
       method="post">
    {% csrf_token %}
    {{ form.as_p }}
    <button type="submit">
      Create Walk</button>
    </form>
  </div>
</div>
